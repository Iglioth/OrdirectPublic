@model OrdirectWebsite.ReserveringViewModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.reserveringDetailViewModels) {
        <tr>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ReserveringID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ReserveringID }) |
            </td>
        </tr>
}
    </tbody>
</table>
